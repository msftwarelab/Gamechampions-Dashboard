const GamesListTheme = ({ colors, fonts }) => {
  return {
    linkTheme: {
      color: colors.grey,
      fontSize: fonts.small,
      textTransform: "uppercase"
    }
  };
};

export default GamesListTheme;
