const PaginationTheme = () => {
  return {
    rootTheme: {
      flex: {
        base: 2,
        md: 1
      },
      justifyContent: "center",
      margin: {
        base: "0 0 4rem",
        md: "0 0 1rem"
      },
      display: "flex",
      flexDirection: "column",
      alignItems: "center"
    }
  };
};

export default PaginationTheme;
