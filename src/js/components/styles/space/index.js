import { css } from "styled-components";
import { PaddingStyle } from "../core/padding";
import { MarginStyle } from "../core/margin";
import { LineHeightStyle } from "../core/lineHeight";

export const SpaceStyle = css`
  ${PaddingStyle};
  ${MarginStyle};
  ${LineHeightStyle};
`;
